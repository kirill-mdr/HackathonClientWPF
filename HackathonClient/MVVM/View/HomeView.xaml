<UserControl x:Class="HackathonClient.MVVM.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HackathonClient.MVVM.View" 
             xmlns:viewmodel="clr-namespace:HackathonClient.MVVM.ViewModel" d:DataContext="{d:DesignInstance Type=viewmodel:HomeViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock Text="Home" FontSize="32" Grid.ColumnSpan="2"/>
        <Button Command="{Binding Test}" Content="Button" HorizontalAlignment="Left" Margin="20,67,0,0" VerticalAlignment="Top" Height="142" Width="273"/>
        <Button Command="{Binding Close}" Content="Button" HorizontalAlignment="Left" Margin="59,385,0,0" VerticalAlignment="Top"/>
        <ContentControl 
                        Margin="10,0,10,10"
                        Content="{Binding CurrentView}"/>
    </Grid>
</UserControl>
